- defaults:
    link:
      relink: true
      create: true
      force: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.config/sway: sway
    ~/.config/waybar: waybar
    ~/.config/mako: mako
    ~/.config/gtk-3.0: gtk-3.0
